#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../markdown2bootstrap/markdown2bootstrap.js" "$@"
  ret=$?
else 
  node  "$basedir/../markdown2bootstrap/markdown2bootstrap.js" "$@"
  ret=$?
fi
exit $ret
